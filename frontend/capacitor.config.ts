import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cryptograph.analytics',
  appName: 'CryptoGraph',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
